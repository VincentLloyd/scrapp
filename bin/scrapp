#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'scrapp'
# extend Scrapp::CLI

session = Scrapp::CLI.new
session.new_session
session.score_again?

$stdout.write "\e[25l"
