#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'scrapp'
extend Scrapp::Interface

Scrapp::Interface.new_session
puts 'something else can happen'

$stdout.write "\e[25l"
